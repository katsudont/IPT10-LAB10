<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Successful Registration</title>

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
            background-color: #e0f7fa; 
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .main__container {
            width: 100%;
            max-width: 500px;
            padding: 30px;
            background-color: #fff;
            border-left: 10px solid #0044cc; 
            border-radius: 8px;
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.1);
            text-align: left;
        }

        h2 {
            font-size: 26px;
            margin-bottom: 15px;
            color: #0044cc; 
        }

        p {
            font-size: 16px;
            margin-bottom: 25px;
            color: #333;
        }

        a {
            text-decoration: none;
            padding: 12px 30px;
            background-color: #ffcc00; 
            color: #0044cc; 
            border-radius: 4px;
            font-size: 18px;
            font-weight: bold;
            display: inline-block;
        }

        a:hover {
            background-color: #ffdd33;
        }
    </style>
</head>
<body>

    <div class="main__container">
        <h2>Registration Successful!</h2>
        <p>Thank you for registering. Your registration was completed successfully. Please proceed to log in.</p>

        <!-- Link to Login -->
        <a href="/login-form">Go to Login</a>
    </div>

</body>
</html>
